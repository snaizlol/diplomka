import 'package:flutter/widgets.dart';

class AddPubPage extends StatelessWidget {
  const AddPubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
